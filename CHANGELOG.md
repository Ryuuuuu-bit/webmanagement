# Changelog

บันทึกการเปลี่ยนแปลงหลักของโปรเจกต์ TeachSchedule เรียงจากใหม่ไปเก่า อ้างอิงจาก git history จริง
(ข้อความสั้น ๆ ต่อ commit ดูรายละเอียดเพิ่มเติมได้จาก `git log`)

## 2026-09 (รอบ 2) — ความปลอดภัยบัญชี, เช็คอินไร้รหัสผ่าน, การแจ้งเตือน

- **เปลี่ยน: ไม่บังคับตั้งรหัสผ่านใหม่ก่อนเข้าแอป** — ผู้ใช้ใหม่/ถูกรีเซ็ตรหัสเข้าใช้งานได้ทันที มีแถบเตือนสีส้มทุกหน้า
  (บอกวันหมดอายุของรหัสชั่วคราว) + การแจ้งเตือนในกระดิ่ง จนกว่าจะตั้งรหัสของตัวเอง หน้าตั้งรหัสมีปุ่ม "ไว้ทีหลัง"
  และหลังเปลี่ยนรหัสระบบต่อ session ให้อัตโนมัติ (login ticket) ไม่ต้อง login ใหม่ คู่มือปรับตาม
- **เพิ่ม: Admin แก้ไขข้อมูลส่วนตัวของอาจารย์ได้ทั้งหมด และลบประวัติการทำรายการได้** — ปุ่ม "แก้ไขข้อมูล" (ชื่อ,
  ชื่อผู้ใช้, อีเมล, ภาควิชา) ในหน้าจัดการผู้ใช้ และหน้าใหม่ `/admin/users/[id]/history` แสดงประวัติเช็คอิน-เอาต์
  (พร้อมรูปเซลฟี่/ธงอุปกรณ์ร่วม), คำขอลา, คำขอรับรองเวลา, แผนการสอน — ลบทีละรายการหรือล้างทั้งหมวดได้
  ทุกการลบ/แก้ไขบันทึกใน Audit Log (`PROFILE_EDITED`, `RECORD_DELETED`)
  หน้าภาพรวมเช็คอิน-เอาต์ของ Admin มีปุ่ม "ลบ" รายการของวันนี้และลิงก์ "ประวัติ" ต่อแถวด้วย
- **เพิ่ม:** อาจารย์เช็คอิน/เช็คเอาต์ได้จาก Dashboard โดยตรง (ปุ่มเดียวกับหน้าเช็คอิน ทุกนโยบายมีผลเหมือนกัน);
  อัปเดต README / CHANGELOG / `.env.example` ให้ตรงกับระบบปัจจุบัน
- **เพิ่ม: การแจ้งเตือนในแอป** (`759d0dc`) — ตาราง `Notification` + กระดิ่งพร้อมตัวเลขในเมนู, หน้า "การแจ้งเตือน",
  และกล่องเด้งเมื่อมีเรื่องใหม่ระหว่างเปิดแอป ครอบคลุมทุก workflow: ยื่นลา/ขอรับรองเวลา/ส่งแผนการสอน/ลงทะเบียนอุปกรณ์
  → แจ้ง Admin ทุกคน; อนุมัติ/ไม่อนุมัติ/ส่งกลับ/อนุมัติอุปกรณ์ → แจ้งอาจารย์; ตรวจพบใช้อุปกรณ์เดียวกันเช็คอินแทนกัน
  → แจ้ง Admin; Admin เพิ่ม/ลบคาบสอน เปลี่ยนสิทธิ์ เปลี่ยน site ประจำ → แจ้งอาจารย์คนนั้น ข้อความ render ตามภาษา
  ของผู้อ่าน เก็บไว้ 90 วัน
- **เปลี่ยน: เอา "ลืมรหัสผ่าน" แบบทำเองออก** (`362ef7e`) — อาจารย์ที่ลืมรหัสผ่านติดต่อผู้ดูแลระบบให้รีเซ็ต
  (`/forgot-password` redirect ไปหน้า login) ปุ่ม "ส่งลิงก์ตั้งรหัสผ่านทางอีเมล" ของ Admin ยังอยู่เมื่อตั้งค่า Resend
  คู่มือใน `docs/` ปรับตาม
- **แก้ไข: อัปโหลดแผนการสอนล้มเหลวบนมือถือ** (`011e65c`) — ย้ายจาก Server Action ไป Route Handler
  `/api/lesson-plans/upload` (XHR + progress) แก้ "Unexpected end of form"; เพิ่ม `requireUser()` ทุกหน้าใน `(app)`
  แก้หน้าล่มเมื่อ session ค้าง; เพิ่มหน้า error แบบเป็นมิตร (`error.tsx`)
- **เพิ่ม:** Admin ลบบัญชี Admin คนอื่นได้ (ต้องเหลืออย่างน้อยตัวเอง) + สคริปต์ลบบัญชีแบบ gated ด้วย token (`87d3e5e`)
- **ปรับ: การแสดงผลบน PC/มือถือ** (`a431a34`) — หน้าจัดการผู้ใช้เป็นการ์ด, ตารางข้อมูลไม่บีบข้อความ (nowrap + เลื่อนแนวนอน),
  แก้ตัวเลือกภาคเรียนในปฏิทินถูกตัด
- **เพิ่ม: ป้องกันการเช็คอินแทนกัน (anti buddy-punching) ฉบับเต็ม** (`4b288f6`) — นโยบายเช็คอินตั้งได้จากหน้า Admin:
  บังคับไบโอเมตริก (ไม่มีทางลัดรหัสผ่าน), อุปกรณ์ใหม่ต้องรออนุมัติ (1 อุปกรณ์/คน), เซลฟี่ตอนเช็คอิน/เอาต์ (เก็บ N วัน),
  ตรวจจับมือถือเครื่องเดียวเช็คอินให้หลายคนแล้วติดธง ⚠; หน้าภาพรวมของ Admin แสดงวิธียืนยัน/รูป/ธง; สคริปต์ seed
  บัญชีตัวอย่าง 3 คน; เพิ่มคู่มือการใช้งานครั้งแรก (`docs/`)
- **เพิ่ม:** สลับ TH/EN และ Light/Dark ได้ตั้งแต่หน้า login และหน้าสาธารณะอื่น (`98cb802`)
- **เปลี่ยน: เข้าสู่ระบบด้วย username** (`8ca6c88`) — Admin ตั้ง username ตอนสร้างบัญชี (backfill ให้บัญชีเดิมจาก
  อีเมลอัตโนมัติ) รับอีเมลแทนได้; เพิ่มส่งอีเมลผ่าน Resend (ไม่บังคับ)
- **เพิ่ม: เช็คอินไร้รหัสผ่านและความปลอดภัยบัญชี** (`84f2bfd`) — passkey login (WebAuthn discoverable credential),
  ลงทะเบียนอุปกรณ์ด้วย QR/ลิงก์ (15 นาที ใช้ครั้งเดียว), คู่มือติดตั้ง PWA ในหน้า login, ล็อกบัญชีใน DB ต่อบัญชี/ต่อ IP,
  นโยบายรหัสผ่าน, รหัสชั่วคราวหมดอายุ 7 วัน, ระงับ/คืนสิทธิ์บัญชี, ออกจากระบบทุกอุปกรณ์, ประวัติความปลอดภัย
  (`/admin/audit`)
- **ปรับ: ปฏิทินตารางสอนสไตล์ Microsoft Teams** (`a163690`) — มุมมองสัปดาห์/วันทำงาน/วัน, กรองตามภาคเรียน,
  เส้นเวลาปัจจุบัน, มินิปฏิทินเลือกวัน
- **เพิ่ม: โควต้าวันลาต่อปีต่อประเภท** (`b30ed92`) — ตั้งได้ในข้อมูลหลัก ขอเกินได้แต่เตือนทั้งสองฝ่าย
- **ปรับ:** รองรับมือถือทั้งแอป (เมนู drawer, layout ยืดหยุ่น) (`21d2234`); ยืนยันตัวตนตอนเช็คอินเวอร์ชันแรก (`f200154`);
  ดึง GPS ล่วงหน้าให้เช็คอินเร็วขึ้น (`7d0d386`)

## 2026-09 — ระบบหลาย Site / อาจารย์ประจำ site ของตัวเอง

- **แก้ไข: เอาตัวอย่างชื่อสถานที่จริงออกจาก placeholder ทั้งเว็บ** — ช่องค้นหา/กรอกจุดเช็คอิน (ชื่อ, ละติจูด,
  ลองจิจูด, ช่องค้นหาสถานที่) ไม่มีข้อความตัวอย่างที่อ้างอิงสถานที่จริง (เช่น "มหาวิทยาลัยเกษตรศาสตร์") อีกต่อไป
  เปลี่ยนเป็นข้อความอธิบายทั่วไปแทน (ทั้งภาษาไทยและอังกฤษ)
- **เพิ่ม: อาจารย์แต่ละคนมี "site ประจำ" ของตัวเอง** (`User.campusLocationId`) — Admin กำหนด site ให้อาจารย์แต่ละคน
  ได้จากหน้าจัดการผู้ใช้ (ตอนสร้างบัญชี หรือแก้ไขทีหลังก็ได้) การเช็คอิน/เช็คเอาต์ตรวจสอบกับ site ที่อาจารย์คนนั้น
  ถูกกำหนดไว้โดยตรง แทนที่การอ้างอิงจากตารางสอน/ห้องเรียนแบบเดิม รองรับ concept ที่ลูกค้าต้องการ: มีอาจารย์หลายคน
  กระจายกันไปประจำ site หลายจุด ไม่ได้จำกัดอยู่ที่มหาวิทยาลัยเดียวอีกต่อไป
  - ไม่มี site ประจำ = เช็คอิน/เช็คเอาต์ไม่ได้ (กันเช็คอินจาก "ที่ไหนก็ได้" โดยไม่ตั้งใจ)
  - หน้า "รายชื่ออาจารย์" และหน้าภาพรวมเช็คอินของ Admin แสดงคอลัมน์ "Site" ของแต่ละคน
  - ข้อความในเว็บที่เคยระบุเฉพาะเจาะจง (เช่น ชื่อมหาวิทยาลัย) ถูกปรับให้เป็นกลางมากขึ้น
- **ปรับ:** หน้าแผนที่จุดเช็คอิน-เช็คเอาต์ของ Admin ให้ดูสอดคล้องกันทั้งรูปแบบและพฤติกรรม (การเลือกพิกัด, รัศมี,
  รูปแบบการแสดงผล)
- เปลี่ยนบริการค้นหาชื่อสถานที่จาก Nominatim → Geoapify → **Google Places API** (แม่นยำกว่าสำหรับชื่อสถานที่ไทย)
- เพิ่มการคลิกบนแผนที่เพื่อปักหมุดกำหนดพิกัดจุดเช็คอินได้โดยตรง
- เพิ่มแผนที่แบบ interactive (Leaflet + OpenStreetMap) ในหน้าจัดการจุดเช็คอิน-เช็คเอาต์

## 2026 (ต้นปี–กลางปี) — เช็คอินตามตำแหน่ง, PWA, และการใช้งานหลายภาษา

- เพิ่มการตรวจสอบเช็คอิน/เช็คเอาต์ตามตารางสอนของแต่ละอาจารย์ (เวอร์ชันแรก อิงห้อง/ตารางสอนรายวัน — ภายหลังถูกแทนที่
  ด้วยระบบ site ประจำต่ออาจารย์ด้านบน)
- เพิ่มการรองรับ PWA (ติดตั้งเป็นแอปได้) และขยายอายุ session ให้เช็คอินได้สะดวกขึ้นโดยไม่ต้อง login ใหม่บ่อย ๆ
- ตั้งชื่อเว็บไซต์และเพิ่ม favicon
- แก้ปัญหา hydration mismatch (React error #418/#423/#425/#329) ทั่วทั้งเว็บ
- แก้ server error ที่หน้า ตารางสอน / เช็คอิน / รับรองเวลา / ลา / แผนการสอน / ข้อมูลหลัก
- เพิ่มระบบสลับภาษาไทย/อังกฤษแบบเต็มรูปแบบ (ไม่ใช่แค่บางส่วน)
- เพิ่มปุ่มสลับโหมด Light/Dark ทั้งเว็บ
- แสดงชื่อและสิทธิ์ของผู้ใช้ที่ล็อกอินอยู่ใน sidebar

## ช่วงกลาง — ตารางสอนแบบยืดหยุ่น และ Master Data

- เพิ่มช่องบันทึกหมายเหตุ/รายละเอียดในปฏิทินตารางสอน, แก้การรับรองเวลาให้รองรับกรณี "ลืมบันทึกทั้งเข้าและออก",
  จัดรูปแบบวันที่/เวลาแบบไทย
- เปลี่ยนจากตารางสอนแบบคาบเวลาตายตัว เป็นปฏิทินแบบเวลาอิสระสไตล์ MS Teams
- รองรับการจัดตารางสอนวันเสาร์-อาทิตย์
- แก้ปัญหาเขตเวลาให้ตรงกับประเทศไทย และเปิดให้อาจารย์เพิ่มตารางสอนของตัวเองได้
- เพิ่มการจัดการ Master Data โดย Admin (สาขาวิชา, รายวิชา, ห้อง, ภาคเรียน)
- เพิ่มประเภทการลาตามกฎหมายที่เหลือ (ลาคลอด, ทำหมัน, เกณฑ์ทหาร, ฝึกอบรม)
- เพิ่มฟีเจอร์ส่งแผนการสอน (Lesson Plan) พร้อมขั้นตอนอนุมัติ
- เพิ่มการล็อกอินแบบจำกัดจำนวนครั้ง (rate limiting), การยกเลิก session เมื่อรีเซ็ตรหัสผ่าน/เปลี่ยนสิทธิ์,
  และ Admin ลบบัญชีผู้ใช้ได้
- เพิ่มข้อมูลพิกัดจริงของสถานที่และให้ Admin จัดการจุดเช็คอินได้เองผ่านหน้าเว็บ

## ช่วงต้น — โครงสร้างระบบและบัญชีผู้ใช้

- เปลี่ยนจากระบบสมัครสมาชิกเอง เป็นระบบปิดที่ Admin สร้างบัญชีให้เท่านั้น (ตามที่ใช้งานจริงในปัจจุบัน)
- เพิ่มระบบจัดการสิทธิ์ (Admin/Member) และสร้างบัญชี Admin คนแรกผ่านขั้นตอน bootstrap
- ปรับ deploy ให้ใช้ `prisma db push` (ไม่มีประวัติ migration แยก) เหมาะกับการ deploy ต่อเนื่องบน Railway
- โครงสร้างเริ่มต้น: Next.js (App Router) + Prisma + NextAuth สำหรับระบบตารางสอนอาจารย์

---

รายละเอียดการเปลี่ยนแปลงเชิงเทคนิคเพิ่มเติม (เช่น เหตุผลการออกแบบ, สคีมาฐานข้อมูล) บันทึกไว้ใน
[claude.ai Project "Project Management"](https://claude.ai) — เอกสารที่เกี่ยวข้อง: `in-app-notifications-and-admin-only-password-reset.md`,
`anti-buddy-punching-v2-and-demo-reset.md`, `username-login-and-email-password-reset.md`,
`passwordless-checkin-and-account-security.md`, `teams-style-calendar.md`, `mobile-responsive-pass.md`,
`site-based-checkin-feature.md`, `location-picker-map-feature.md`, `pwa-checkin-feature.md`, `ivt-pvt-report-teachschedule.md`
