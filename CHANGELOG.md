# Changelog

สรุปสั้นต่อรอบ (ใหม่→เก่า) รายละเอียดดู `git log`

## 2026-09 รอบ 3
- จัดตารางสอน: ตัวเลือกห้องแสดงเฉพาะห้องใน site ประจำของอาจารย์ (และห้องที่ยังไม่กำหนด site) พร้อมตรวจซ้ำฝั่งเซิร์ฟเวอร์
- แก้: ปิดนโยบาย "บังคับ Face ID" แล้วไม่ต้องสแกนจริง (ใช้ session ที่ login อยู่ ไม่มีช่องรหัสผ่านอีกต่อไป)
- เวลาทำงานตั้งได้: ค่ามาตรฐานในข้อมูลหลัก (เริ่ม/เลิก/นาทีผ่อนผัน) และตั้งทับรายจุดเช็คอินได้ → สาย = หลังเริ่ม+ผ่อนผัน, เช็คเอาต์ก่อนเลิกติดป้าย "ออกก่อนเวลา"
- ค้นหา/กรองในทุกหน้ารายการ (ประวัติความปลอดภัย: เหตุการณ์+ช่วงวันที่ และดูได้ถึง 2000 รายการ, ภาพรวมเช็คอิน, การลา, รับรองเวลา, แผนการสอน, รายชื่ออาจารย์, ผู้ใช้, ประวัติต่อคน, การแจ้งเตือน)
- ปุ่มเช็คอิน/เช็คเอาต์แบบใหม่: การ์ดใหญ่ 2 ช่อง แสดงขั้นถัดไป/เวลาที่ทำแล้ว
- แก้: อาจารย์ที่ผูกเครื่องด้วย QR (ไม่เคยรู้รหัสชั่วคราว) ตั้งรหัสผ่านเองได้โดยไม่ต้องกรอกรหัสเดิม (`User.passwordSetAt`); รหัสชั่วคราวยังหมดอายุตามกำหนด
- Web Push: เปิด "แจ้งเตือนบนมือถือ" ในหน้าการแจ้งเตือน แล้วแจ้งเตือนเด้งแม้ปิดแอป (VAPID env; iPhone ต้องติดตั้งบนหน้าจอโฮม)
- Admin แก้ไขเวลาเช็คอิน-เอาต์/สถานะได้ (ทำเครื่องหมายรับรองเวลา + Audit) ทั้งหน้าภาพรวมและหน้าประวัติ
- การลา: แนบไฟล์ (PDF/รูป ≤5 MB), ลาครึ่งวัน (0.5 วันในโควต้า), ยกเลิกคำขอเองขณะรออนุมัติ (สถานะ CANCELLED, แจ้งแอดมิน)
- นำเข้าผู้ใช้จาก Excel/CSV (หัวคอลัมน์ไทย/อังกฤษ, ไฟล์ตัวอย่าง, รหัสชั่วคราวต่อคน, ดาวน์โหลดผลลัพธ์)
- ประวัติความปลอดภัยมีคอลัมน์ "อุปกรณ์" (รหัสติดตั้ง 8 ตัว + ระบบ/เบราว์เซอร์) แทนการพึ่ง IP
- หน้ายินยอม PDPA ครั้งแรกหลัง login (บันทึกวันที่/เวอร์ชัน, แสดงในหน้าจัดการผู้ใช้)

## 2026-09 รอบ 2
- ไม่บังคับตั้งรหัสใหม่ก่อนเข้าแอป — แถบเตือน + แจ้งเตือนจนกว่าจะตั้ง; เปลี่ยนรหัสแล้วไม่ต้อง login ใหม่
- Admin แก้ไขข้อมูลอาจารย์ทุกช่อง; หน้าประวัติต่อคนลบเช็คอิน/ลา/รับรองเวลา/แผนการสอนได้ (ทีละรายการ/ทั้งหมวด, ลง Audit)
- อาจารย์เช็คอิน-เอาต์จาก Dashboard ได้; README/CHANGELOG/.env.example ปรับใหม่
- ระบบแจ้งเตือนในแอปครอบคลุมทุก workflow (กระดิ่ง, หน้าแจ้งเตือน, toast, TH/EN ตามผู้อ่าน)
- เอา "ลืมรหัสผ่าน" แบบทำเองออก (แอดมินรีเซ็ต)
- แก้อัปโหลดแผนการสอนล้มบนมือถือ (Route Handler + XHR); `requireUser()` ทุกหน้า; หน้า error แบบเป็นมิตร
- กันเช็คอินแทนกัน: บังคับไบโอเมตริก, อุปกรณ์รออนุมัติ, เซลฟี่, ตรวจจับมือถือเครื่องเดียวหลายบัญชี; คู่มือใน `docs/`
- Passkey login, QR ผูกเครื่อง, username login, ล็อกบัญชี, นโยบายรหัสผ่าน, ระงับบัญชี, Audit Log, TH/EN บนหน้า login
- ปฏิทินสไตล์ Teams; โควต้าวันลาต่อปี; responsive มือถือ; หน้าจัดการผู้ใช้เป็นการ์ด

## 2026-09 รอบ 1 — หลาย site
- อาจารย์มี site ประจำ ใช้ตรวจเช็คอิน-เอาต์แทนการอิงตารางสอน; แผนที่ Leaflet + Google Places; ปักหมุดบนแผนที่

## ก่อนหน้า
- PWA + session ยาว; TH/EN; Light/Dark; แก้ hydration; ตารางสอนเวลาอิสระ + เสาร์-อาทิตย์; Master Data; ประเภทการลาตามกฎหมาย;
  แผนการสอน + อนุมัติ; ระบบปิด Admin สร้างบัญชี; deploy ด้วย `prisma db push` บน Railway
