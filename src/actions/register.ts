// Self-registration has been removed — this is a closed system where an
// admin creates accounts directly (see src/actions/users.ts and
// /admin/users). Kept as an empty file rather than deleted so any stray
// import fails loudly at build time instead of silently resolving to a
// stale copy.
export {};
