// Deprecated: replaced by ScheduleCalendar.tsx (the MS-Teams-style calendar
// view on /schedule). Left as an empty placeholder rather than deleted,
// since removing files isn't available through this deploy path.
export {};
